import { BatchDirective } from './batch.directive';

describe('BatchDirective', () => {
  it('should create an instance', () => {
    const directive = new BatchDirective();
    expect(directive).toBeTruthy();
  });
});
